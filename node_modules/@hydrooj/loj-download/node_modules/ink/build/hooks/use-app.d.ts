/**
 * `useApp` is a React hook, which exposes a method to manually exit the app (unmount).
 */
declare const useApp: () => import("@hydrooj/loj-download/node_modules/ink/build/components/AppContext").Props;
export default useApp;
