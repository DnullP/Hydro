import { type Queue, type RunFunction } from '@hydrooj/loj-download/node_modules/p-queue/dist/queue.js';
import { type QueueAddOptions } from '@hydrooj/loj-download/node_modules/p-queue/dist/options.js';
export type PriorityQueueOptions = {
    priority?: number;
} & QueueAddOptions;
export default class PriorityQueue implements Queue<RunFunction, PriorityQueueOptions> {
    #private;
    enqueue(run: RunFunction, options?: Partial<PriorityQueueOptions>): void;
    dequeue(): RunFunction | undefined;
    filter(options: Readonly<Partial<PriorityQueueOptions>>): RunFunction[];
    get size(): number;
}
